# Chat Inviter - Инвайты в группы-мероприятия

Модуль для приглашения пользователей из ЦА в группы-мероприятия.

---

## Как это работает

1. Создаём группу с привлекательным названием ("Новая профессия 2026")
2. Система инвайтит людей из ЦА в эту группу
3. Когда набирается N участников — публикуется оффер с ссылкой на воронку
4. Люди переходят по ссылке → лиды

---

## Создание группы

### Из шаблона

```bash
python scripts/create_event_group.py --account karina --template profession_2026
```

### Доступные шаблоны

| Шаблон | Название группы |
|--------|-----------------|
| `profession_2026` | Новая профессия 2026 |
| `remote_income` | Удалённый доход |
| `online_business` | Онлайн-бизнес с нуля |

### Показать шаблоны

```bash
python scripts/create_event_group.py --list-templates
```

### Интерактивное создание

```bash
python scripts/create_event_group.py --account karina
```

---

## Настройки в .env

```env
# Инвайты в группы-мероприятия
MAX_INVITES_PER_DAY=0           # 0 = отключено (прогрев)
MIN_INVITE_INTERVAL_SEC=300     # 5 минут минимум между инвайтами
MAX_INVITE_INTERVAL_SEC=900     # 15 минут максимум
INVITE_MIN_QUALITY_SCORE=50     # Минимальный quality_score для инвайта
```

### Включение после прогрева

```env
MAX_INVITES_PER_DAY=40          # После 30 дней прогрева
```

---

## График прогрева инвайтов

| День | Инвайтов/день |
|------|---------------|
| 1-14 | 0 (отключено) |
| 15-21| 10 |
| 22-30| 20 |
| 30+  | 40 |

---

## Публикация оффера

Когда в группе набирается N участников, система автоматически:
1. Публикует сообщение с оффером
2. Закрепляет его

### Настройка порога

При создании группы:
```
При скольки участниках опубликовать оффер? [50]: 30
```

### Текст оффера

Настраивается в шаблоне или вручную при создании.

Пример:
```
Друзья, рада что нас уже 50 человек!

Я обещала поделиться как начала зарабатывать на удалёнке.

Записала короткое видео (7 мин) где рассказываю:
- Как ушла из офиса
- Что конкретно делаю
- Сколько реально можно заработать

Смотреть здесь: {funnel_link}

P.S. Видео будет доступно только 48 часов
```

---

## Модуль InviteMonitor

Файл: `traffic_engine/channels/chat_inviter/invite_monitor.py`

### Алгоритм работы

1. Проверка рабочих часов (9:00-23:00)
2. Проверка лимита `MAX_INVITES_PER_DAY`
3. Получение доступного аккаунта
4. Получение активной группы
5. Выбор пользователя из ЦА (quality_score >= 50)
6. Инвайт через Telethon API
7. Проверка порога для публикации оффера
8. Задержка 5-15 минут

### Обработка ошибок

- `UserPrivacyRestrictedError` — пропускаем, пользователь закрыл приватность
- `UserAlreadyParticipantError` — пропускаем, уже в группе
- `FloodWaitError` — ставим аккаунт на cooldown
- `ChatAdminRequiredError` — нужны права админа

---

## Показать существующие группы

```bash
python scripts/create_event_group.py --list-groups
```

Вывод:
```
📌 Новая профессия 2026
   ID: -1001234567890
   Статус: ✅ Активна
   Инвайтов: 45
   Вступило: 32
   ⏳ Ждём 50 участников
```

---

## Таблица в БД: InviteChat

| Поле | Тип | Описание |
|------|-----|----------|
| id | int | PK |
| tenant_id | int | FK на Tenant |
| chat_id | bigint | Telegram ID группы |
| title | str | Название |
| invite_link | str | Ссылка-приглашение |
| is_active | bool | Активна ли группа |
| total_invited | int | Всего приглашено |
| total_joined | int | Всего вступило |
| offer_message | text | Текст оффера |
| publish_offer_at_members | int | Порог для публикации |
| offer_published | bool | Опубликован ли оффер |

---

## Ожидаемые результаты

**На 5 аккаунтов, 40 инвайтов/день:**

| Метрика | Значение |
|---------|----------|
| Инвайтов/день | 200 |
| Вступает | ~50% = 100 |
| Переходит по ссылке | ~30% = 30 |
| Лидов/мес | ~300 |

---

**Дата обновления:** 31 января 2026
