# ✅ Чеклист запуска автокомментирования

## 📋 Подготовка (один раз)

### 1. Зависимости
```bash
cd "c:\Users\mafio\OneDrive\Документы\projects\info-business"
venv\Scripts\activate
pip install -r requirements.txt
```

**Проверка:**
```bash
python -c "import telethon; print('Telethon OK')"
python -c "import rich; print('Rich OK')"
```

Должно вывести:
```
Telethon OK
Rich OK
```

---

### 2. База данных PostgreSQL

**Проверка:**
```bash
psql -U postgres -c "SELECT 1 FROM pg_database WHERE datname = 'info_business';"
```

Если БД не существует:
```bash
psql -U postgres -c "CREATE DATABASE info_business;"
```

---

### 3. Создать таблицы и тенанта

```bash
python scripts\init_db.py
```

**Ожидаемый вывод:**
```
Creating database tables...
Tables created successfully!
Seeding tenants...
Created tenant: infobusiness
Created tenant: nl_international
Tenants seeded successfully!
=== Database initialization complete! ===
```

**Проверка:**
```bash
psql -U postgres -d info_business -c "SELECT name FROM traffic_tenants;"
```

Должно показать:
```
      name
-----------------
 infobusiness
 nl_international
```

---

### 4. Добавить аккаунты в БД

**Проверка сессий:**
```bash
ls sessions/
```

Должны быть:
- ✅ account1.session (Лёша)
- ✅ karina.session (Карина)
- ✅ ua_account1.session (Люба)
- ✅ ua_account2.session (Кира)

**Если сессий нет или названия другие - переименуйте:**
```bash
# Пример:
mv sessions/один.session sessions/account1.session
mv sessions/два.session sessions/karina.session
# и т.д.
```

**Добавить в БД:**
```bash
python scripts\setup_accounts.py
```

**Ожидаемый вывод:**
```
Добавлен: Лёша (@lemonlime192) - account1.session
Добавлен: Карина (@karinko_o) - karina.session
Добавлен: Люба (@lyuba_ok) - ua_account1.session
Добавлен: Кира (@kirushka_94) - ua_account2.session

✅ Добавлено аккаунтов: 4
```

**Проверка:**
```bash
psql -U postgres -d info_business -c "SELECT first_name, username FROM traffic_userbot_accounts;"
```

Должно показать 4 аккаунта.

---

### 5. Добавить целевые каналы

```bash
python scripts\setup_channels.py
```

**Ожидаемый вывод:**
```
Получаю данные канала @lentachold...
  ID: -1001234567890, Название: Лента Холд
Добавлен: @lentachold (приоритет: 10)
...
✅ Добавлено каналов: 31
```

**Проверка:**
```bash
psql -U postgres -d info_business -c "SELECT username, priority FROM traffic_target_channels ORDER BY priority DESC;"
```

Должно показать 31 канал:
- 16 про бизнес и предпринимательство
- 10 про мотивацию и саморазвитие
- 5 про финансы и заработок

---

## 🚀 Запуск

### 6. Запустить автокомментирование

```bash
python run_auto_comments.py
```

**Ожидаемый вывод (первые 30 секунд):**
```
============================================================
TRAFFIC ENGINE - АВТОКОММЕНТИРОВАНИЕ
============================================================

Аккаунты для ротации:
  1. Лёша Лаймов (@lemonlime192)
  2. Карина (@karinko_o)
  3. Люба (@lyuba_ok)
  4. Кира (@kirushka_94)

Для остановки нажмите Ctrl+C
============================================================

19:30:15 | INFO     | База данных инициализирована
19:30:16 | INFO     | Запускаю Traffic Engine для тенанта: Инфобизнес - Курсы по заработку
19:30:17 | INFO     | Starting tenant: Инфобизнес - Курсы по заработку
19:30:18 | INFO     | Channel monitor initialized with 31 channels
19:30:19 | INFO     | Starting channel monitor...
19:30:20 | INFO     | Telethon client connected
19:30:21 | INFO     | Already in channel: Лента Холд
```

✅ **РАБОТАЕТ!** Если видите эти сообщения - система запустилась корректно.

Теперь она будет:
- Каждые 30-90 сек проверять новые посты
- Генерировать комментарии через YandexGPT
- Публиковать с ротацией аккаунтов

**Оставьте работать и перейдите к шагу 7.**

---

## 📊 Мониторинг

### 7. Проверить статистику (через 15-30 минут)

Откройте **новый терминал** (не закрывая предыдущий):

```bash
cd "c:\Users\mafio\OneDrive\Документы\projects\info-business"
venv\Scripts\activate
python scripts\check_stats.py
```

**Ожидаемый вывод:**
```
============================================================
TRAFFIC ENGINE - СТАТИСТИКА
============================================================

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃         📊 Статистика за сегодня        ┃
┡━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ ✓ Комментариев отправлено: 5           │
│ ⏸ FloodWait случаев: 0                 │
│ ✗ Ошибок: 0                            │
└─────────────────────────────────────────┘
```

✅ **Если видите отправленные комментарии - система работает!**

---

### 8. Проверить реальные комментарии в Telegram

Откройте любой канал из списка (например, @lentachold) и посмотрите комментарии под свежими постами.

Должны быть комментарии от ваших аккаунтов:
- @lemonlime192
- @karinko_o
- @lyuba_ok
- @kirushka_94

✅ **Если комментарии появились - ОТЛИЧНО! Система полностью работает!**

---

## 🔁 Дальнейшая работа

### 9. Оставить на весь день

Система будет работать автоматически:
- ✅ С 9:00 до 23:00
- ✅ Максимум 80 комментов/день на аккаунт
- ✅ С интервалом 30-180 секунд
- ✅ С ротацией 4 аккаунтов

**Проверяйте статистику каждые 2-3 часа:**
```bash
python scripts\check_stats.py
```

---

### 10. Логи

**Смотреть логи в реальном времени:**
```bash
tail -f logs/auto_comments_*.log
```

**Или на Windows (Git Bash):**
```bash
Get-Content logs\auto_comments_*.log -Wait -Tail 50
```

---

## ⚠️ Возможные проблемы

### "No available accounts for commenting"

**Причина:** Все аккаунты достигли дневного лимита или в cooldown.

**Решение:**
- Подождать до следующего дня (лимит сбросится в 00:00)
- Или добавить больше аккаунтов

---

### "FloodWait: 3600s for account"

**Это нормально!** Telegram ограничил аккаунт.

**Действия:**
- ✅ Система автоматически поставила аккаунт в cooldown
- ✅ Другие аккаунты продолжат работу
- ❌ НЕ перезапускайте систему

---

### "Failed to generate comment"

**Причина:** Ошибка YandexGPT API.

**Проверка:**
```bash
python -c "from traffic_engine.config import settings; print(settings.yandex_folder_id)"
```

Должно вывести `b1gibb3gjf11pjbu65r3`

---

### Комментарии не появляются в каналах

**Причина:** Канал может:
- Не иметь открытых комментариев
- Ограничить комментарии (подписчики >X дней)

**Решение:**
1. Проверьте канал вручную - есть ли там комментарии от других
2. Подождите несколько дней (аккаунты новые)
3. Добавьте другие каналы через `scripts\add_target_channels.py`

---

## ✅ Финальная проверка

Через 1 час после запуска:

- [ ] Система работает без ошибок
- [ ] В статистике >= 3 комментария отправлено
- [ ] В Telegram видны комментарии от аккаунтов
- [ ] Нет постоянных ошибок в логах

**Если всё ✅ - ПОЗДРАВЛЯЮ! Система запущена и работает!**

---

## 🚀 Следующие шаги

После 3-7 дней стабильной работы:

1. **Добавить больше каналов** (до 20-30 целевых)
2. **Масштабировать аккаунты** (до 10-15 аккаунтов)
3. **Stories Reactor** - просмотр сторис ЦА
4. **Chat Inviter** - инвайты в чаты
5. **Admin Bot** - управление через Telegram

Удачи! 🎉
