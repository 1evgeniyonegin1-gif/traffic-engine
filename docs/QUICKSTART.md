# 🚀 Быстрый запуск автокомментирования

## 1️⃣ Подготовка (один раз)

### Проверить, что БД создана

```bash
# Подключиться к PostgreSQL
psql -U postgres

# Создать БД (если нет)
CREATE DATABASE info_business;

# Выйти
\q
```

### Создать таблицы и добавить тенанта

```bash
cd "c:\Users\mafio\OneDrive\Документы\projects\info-business"
venv\Scripts\activate

python scripts\init_db.py
```

### Добавить аккаунты в БД

```bash
python scripts\setup_accounts.py
```

Это добавит 4 аккаунта:
- ✅ Лёша Лаймов (@lemonlime192)
- ✅ Карина (@karinko_o)
- ✅ Люба (@lyuba_ok)
- ✅ Кира (@kirushka_94)

### Добавить целевые каналы

```bash
python scripts\setup_channels.py
```

Это добавит **31 целевой канал**:
- 16 каналов про бизнес и предпринимательство
- 10 каналов про мотивацию и саморазвитие
- 5 каналов про финансы и заработок

---

## 2️⃣ Запуск автокомментирования

```bash
cd "c:\Users\mafio\OneDrive\Документы\projects\info-business"
venv\Scripts\activate

python run_auto_comments.py
```

### Что будет происходить:

1. **Каждые 30-90 секунд** система проверяет целевые каналы
2. **Находит новые посты** с открытыми комментариями
3. **Фильтрует** (пропускает рекламу, репосты, короткие посты)
4. **Генерирует умный комментарий** через YandexGPT
5. **Публикует комментарий** от одного из 4 аккаунтов (ротация)
6. **Ждёт 30-180 секунд** перед следующим комментарием

### Лимиты безопасности:

- **Максимум 80 комментариев/день** на аккаунт
- **Интервал 30-180 секунд** между комментариями
- **Рабочие часы:** 9:00 - 23:00
- **Автоматический cooldown** при FloodWait

---

## 3️⃣ Мониторинг

### Логи в терминале

```
19:30:15 | INFO     | Starting tenant: Инфобизнес - Курсы по заработку
19:30:16 | INFO     | Found 2 new posts in @lentachold
19:30:18 | INFO     | Posted comment to channel -1001234567890, post 89016 via account +380950182582
```

### Логи в файлах

```
logs/auto_comments_2026-01-22.log
logs/traffic_engine_2026-01-22.log
```

### Остановка

```
Ctrl + C
```

---

## 4️⃣ Проверка работы

### Посмотреть статистику в БД

```bash
python scripts\check_stats.py
```

Это покажет:
- Сколько комментариев отправлено
- Какие аккаунты работают
- Статус каждого аккаунта
- Последние действия

---

## 🔧 Настройки

### Изменить лимиты

Отредактируйте [.env](.env):

```env
MAX_COMMENTS_PER_DAY=80          # Комментариев на аккаунт/день
MIN_COMMENT_INTERVAL_SEC=30      # Минимальная пауза между комментами
MAX_COMMENT_INTERVAL_SEC=180     # Максимальная пауза
WORK_START_HOUR=9                # Начало рабочего дня
WORK_END_HOUR=23                 # Конец рабочего дня
```

### Добавить новый канал для комментирования

```bash
python scripts\add_target_channels.py
```

Введите username канала (без @) и приоритет (1-10).

---

## ❓ Частые вопросы

### "No available accounts for commenting"

**Причина:** Все аккаунты достигли дневного лимита или в cooldown.

**Решение:**
1. Проверить логи: `tail -f logs/auto_comments_*.log`
2. Подождать до следующего дня (лимит сбросится в 00:00)
3. Или увеличить `MAX_COMMENTS_PER_DAY` в `.env`

### "Failed to get client for account"

**Причина:** Сессия аккаунта устарела или не найдена.

**Решение:**
1. Проверить, что файл сессии существует: `ls sessions/`
2. Переавторизовать аккаунт: `python scripts\auth_one.py`

### "FloodWait: 3600s for account"

**Причина:** Telegram ограничил аккаунт за слишком частые действия.

**Решение:**
- Это нормально! Система автоматически поставит аккаунт в cooldown
- Используйте другие аккаунты
- Уменьшите `MAX_COMMENTS_PER_DAY` в `.env`

### Комментарии не появляются в каналах

**Причина:** Канал может:
- Не иметь открытых комментариев
- Ограничить, кто может комментировать (подписчики >X дней)
- Быть приватным

**Решение:**
1. Проверить канал вручную - есть ли там комментарии
2. Убедиться, что аккаунты подписаны на канал несколько дней
3. Выбрать другие каналы через `setup_channels.py`

---

## 📊 Следующие шаги

После того, как автокомментирование заработает:

1. **Stories Reactor** - просмотр сторис ЦА + реакции
2. **Chat Inviter** - приглашения в чаты с оффером
3. **Admin Bot** - Telegram бот для управления через команды

---

## 🆘 Помощь

Проблемы? Создайте issue в репозитории или напишите в Telegram.
