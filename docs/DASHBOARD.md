# Dashboard - Веб-интерфейс мониторинга

Веб-дашборд для мониторинга всех действий Traffic Engine в реальном времени.

---

## Запуск

```bash
cd traffic-engine-mvp

# Активировать venv
..\venv\Scripts\activate

# Запустить дашборд
python dashboard/app.py

# Открыть в браузере
http://localhost:8050
```

---

## Что показывает

### Статистика за сегодня
- Комментарии (всего / успешных)
- Просмотры сторис + реакции
- Инвайты (всего / успешных)
- Активные аккаунты
- Success Rate

### Аккаунты
| Колонка | Описание |
|---------|----------|
| Account | Имя + username |
| Phone/Country | Номер телефона + флаг страны (🇮🇩 🇺🇦 🇷🇺 и др.) |
| Proxy | Статус: ✅ socks5 + адрес или ❌ No proxy + ⚠️ Risk of ban! |
| Status | active, warming, banned, cooldown |
| Comments | Комментариев сегодня |
| Stories | Просмотров / реакций сегодня |
| Invites | Инвайтов сегодня |
| Last activity | Время последнего действия |

**Визуальные предупреждения:**
- Красный фон строки = иностранный аккаунт без прокси
- "⚠️ Risk of ban!" = явное предупреждение о риске бана

### Последние действия
- **Комментарии** — аккаунт, канал, текст, статус, время
- **Сторис** — аккаунт, пользователь, реакция, статус, время
- **Инвайты** — аккаунт, пользователь, группа, статус, время

---

## API Endpoints

| Endpoint | Описание |
|----------|----------|
| `GET /` | Главная страница (HTML) |
| `GET /api/stats` | Статистика за сегодня |
| `GET /api/accounts` | Список аккаунтов |
| `GET /api/comments` | Последние 50 комментариев |
| `GET /api/stories` | Последние 50 просмотров сторис |
| `GET /api/invites` | Последние 50 инвайтов |

### Пример ответа /api/stats

```json
{
    "comments_today": 25,
    "comments_success": 23,
    "stories_today": 50,
    "reactions_today": 15,
    "invites_today": 10,
    "invites_success": 8,
    "total_accounts": 5,
    "active_accounts": 4,
    "success_rate": 85
}
```

---

## Технические детали

- **Framework:** Flask 3.0
- **Порт:** 8050
- **Авто-обновление:** каждые 30 сек
- **База данных:** PostgreSQL (та же что у Traffic Engine)

---

## Обработка ошибок

Все API endpoints обёрнуты в try/except:
- При ошибке возвращается `{"error": "..."}` с кодом 500
- Ошибки логируются через loguru

---

## Настройка порта

```python
# dashboard/app.py
app.run(host='0.0.0.0', port=8050)  # Изменить порт здесь
```

---

## Безопасность

**Дашборд НЕ имеет аутентификации!**

Для продакшена рекомендуется:
1. Запускать только на localhost
2. Использовать VPN/туннель для удалённого доступа
3. Добавить HTTP Basic Auth

---

## Связанные скрипты

### Управление прокси
```bash
# Показать все аккаунты с прокси
python scripts/manage_proxy.py --list

# Добавить прокси
python scripts/manage_proxy.py --add --phone "+62xxx" --proxy "socks5://user:pass@ip:port"

# Тест прокси
python scripts/manage_proxy.py --test --phone "+62xxx"
```

---

**Дата обновления:** 31 января 2026
